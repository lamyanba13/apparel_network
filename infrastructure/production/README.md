# Production environment

Production infrastructure is intentionally absent. OpenTofu, private networking, managed services, secrets, backups, and monitoring require the hosting-provider ADR and protected deployment workflow.

