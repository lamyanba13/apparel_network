# Shared workspace packages

Narrow generated API-client, UI primitive, and shared configuration packages may be introduced when their owning phase begins. Phase 1.1 intentionally creates no shared feature logic or publishable package.

